services:
    backend:
        build: ./backend
        container_name: backend
        expose:
            - "5000"

    frontend:
        build: .
        ports:
            - "8080:80"
        depends_on:
            - backend
